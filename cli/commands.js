// Defines CLI commands to be registered with commander

export function setupCommands(program) {
  // Stub: register commands here in later implementation
  // e.g. program.command('generate').description('Generate unique item')
}
